{
  "name": "agent-bark-notify",
  "version": "0.2.0",
  "description": "Bark push notification to iPhone/Apple Watch when Claude Code finishes a turn. Also supports Codex CLI.",
  "author": {
    "name": "pamler1004",
    "url": "https://github.com/pamler1004"
  },
  "userConfig": {
    "bark_key": {
      "type": "string",
      "title": "Bark device key",
      "description": "打开 iPhone 上的 Bark app，复制 device key（URL 里 api.day.app/ 后面那串）",
      "sensitive": true,
      "required": true
    },
    "bark_server": {
      "type": "string",
      "title": "Bark server URL",
      "description": "自建 Bark 服务地址；留空用官方 https://api.day.app",
      "default": "https://api.day.app"
    },
    "sound": {
      "type": "string",
      "title": "Sound name (optional)",
      "description": "iOS 提示音名（如 calypso）；留空用 Bark app 默认",
      "default": ""
    },
    "icon": {
      "type": "string",
      "title": "Icon URL (optional)",
      "description": "通知图标图片 URL（HTTPS）；留空用 Bark app 默认图标",
      "default": ""
    }
  }
}
